<h1>Articles</h1>

<ul>
  <% @articles.each do |article| %>
    <li>
      <%= link_to article.title, article %>
    </li>
  <% end %>
</ul>

<%= link_to "New Article", new_article_path %>
<%= button_to "Logout", logout_path, data: {
  turbo_method: :delete,
  turbo_confirm: "Are you sure?"
} %>
